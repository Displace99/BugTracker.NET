name: EZRep Build

on:
  push:
    branches: master

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: Build Solution
        run: |
          msbuild.exe src\BugTracker.NET.sln
